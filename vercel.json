{
  "buildCommand": "cd ../.. && pnpm prisma generate && pnpm prisma migrate deploy && cd apps/web && pnpm build",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && pnpm install",
  "framework": "nextjs",
  "regions": ["syd1"],
  "rootDirectory": "apps/web"
}

