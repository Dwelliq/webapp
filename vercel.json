{
  "buildCommand": "pnpm prisma generate && pnpm prisma db push --skip-generate && cd apps/web && pnpm build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "regions": ["syd1"]
}

